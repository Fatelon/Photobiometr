export interface IApiPictureObject {
  name: string;
  ratio: number;
  imgPath: string;
  thumbnailsPath: string;
}

export interface IPictureObject {
  name: string;
  thumb: string;
  photo: string;
}
